diff --git a/tcg/wasm32.h b/tcg/wasm32.h
--- a/tcg/wasm32.h
+++ b/tcg/wasm32.h
@@ -38,5 +38,6 @@ int get_core_nums();
 void init_wasm32();
 
-#define INSTANTIATE_NUM 1500
+/* Warm a block briefly, then promote it to native WebAssembly. */
+#define INSTANTIATE_NUM 25
 
 #endif
diff --git a/tcg/wasm32.c b/tcg/wasm32.c
--- a/tcg/wasm32.c
+++ b/tcg/wasm32.c
@@ -96,10 +96,14 @@ EM_JS(int, instantiate_wasm, (), {
 
         Module.__wasm32_tb.inst_gc_registry.register(inst, "instance");
 
-        const fidx = addFunction(inst.exports.start, 'ii');
+        /* Force Emscripten to create a table entry with the C ABI expected by wasm_func_ptr. */
+        const fidx = addFunction((ctx) => inst.exports.start(ctx), 'ii');
 
         return fidx;
 });
+EM_JS(uint32_t, execute_wasm, (int fidx, int ctx_ptr), {
+        return wasmTable.get(fidx)(ctx_ptr);
+});
 
 __thread bool initdone = false;
 __thread int cur_core_num = -1;
@@ -1352,7 +1357,7 @@ uintptr_t QEMU_DISABLE_CFI tcg_qemu_tb_exec(CPUArchState *env,
         uint32_t res;
         int fidx = get_instance_running_local(ctx.tb_ptr);
         if (fidx > 0) {
-            res = ((wasm_func_ptr)(fidx))(&ctx);
+            res = execute_wasm(fidx, (int)&ctx);
         } else if (*(int32_t*)tb_counter_ptr < INSTANTIATE_NUM) {
             *(int32_t*)tb_counter_ptr += 1;
             res = tcg_qemu_tb_exec_tci(env);
@@ -1363,7 +1368,7 @@ uintptr_t QEMU_DISABLE_CFI tcg_qemu_tb_exec(CPUArchState *env,
         } else {
             int fidx = instantiate_wasm();
             add_instance_running_local(fidx, ctx.tb_ptr);
-            res = ((wasm_func_ptr)(fidx))(&ctx);
+            res = execute_wasm(fidx, (int)&ctx);
         }
         if ((uint32_t)ctx.tb_ptr == 0) {
             return res;
